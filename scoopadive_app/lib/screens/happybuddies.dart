import 'package:flutter/material.dart';

class HappyBuddies extends StatelessWidget {
  const HappyBuddies({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Happy Buddies'),
    );
  }
}
